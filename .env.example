# Base URL of the referral API (server/index.js).
#   Local dev: /api  (Vite proxies it to `npm run server` on port 8787)
#   Production: wherever you deploy the server, e.g. https://api.throw.app/api
# Leave unset for demo mode: signups work in the browser but nothing is stored.
VITE_API_URL=/api
